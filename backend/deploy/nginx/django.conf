server {
        listen          80;
        server_name     api.stat-exchange.ru;
        charset         utf-8;
        access_log /tmp/stat-exchange/backend/logs/access.log;
        error_log /tmp/stat-exchange/backend/logs/error.log;


        location /static/ {
            root /tmp/stat-exchange/backend/static/;
    }

        location / {
            include proxy_params;
            proxy_pass http://unix:/tmp/stat-exchange/backend/django.sock;
        }
}
